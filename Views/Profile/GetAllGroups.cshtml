@model IEnumerable<CleaverBrooks1.Models.GroupModel>

@{
    ViewData["Title"] = "GetAllGroups";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h2>Companies</h2>

    <p>
        <a asp-action="CreateGroup">Create New Company</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DisplayName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Description)
                    </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <form asp-controller="Profile" asp-action="DeleteGroup" method="get">
                        <input type="hidden" name="Id" value="@item.Id" />
                        <input type="submit" value="Delete" class="btn btn-default" />
                    </form>
                </td>
            </tr>
    }
        </tbody>
    </table>*@

<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 125%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

    <h2>Companies</h2>

    <p>
        <a class="btn btn-default btn-primary" asp-action="CreateGroup">Create New Company</a>
    </p>

    <table class="table-responsive col-lg-offset-3 alert-success ">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DisplayName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                Delete
            </th>
        </tr>



        @foreach (var items in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => items.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => items.DisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => items.Description)
                </td>

                <td>
                    <form asp-controller="Profile" asp-action="DeleteGroup" method="get">
                        <input type="hidden" name="Id" value="@items.Id" />
                        <input type="submit" value="Delete" class="btn btn-default btn-primary text-right" />
                    </form>
                </td>
            </tr>
        }



    </table>

</body>
</html>

